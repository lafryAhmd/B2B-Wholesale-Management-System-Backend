# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/b2b_wholesale
spring.datasource.username=root
spring.datasource.password=8YasProCode
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# File Upload Settings
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=uploads/products

# Server Port
server.port=8081

# Currency API
app.currency.api-url=https://api.exchangerate-api.com/v4/latest/USD

# Invoice Digital Signature (HMAC-SHA256) — change this secret in production
app.invoice.signing-secret=B2B-WHOLESALE-INV-SIGN-KEY-2026-CHANGE-ME
app.invoice.signing-version=v1